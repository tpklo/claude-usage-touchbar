# Build output — reproducible with `make`
ClaudeTouchBar.app/
*.dSYM/
.build/

# Clawd's pose data is Anthropic's and the source publishes no licence, so it
# is not redistributed here — `make assets` fetches it onto your own machine.
clawd_presets.h

# Local scratch
spec.md
